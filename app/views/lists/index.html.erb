<h1>Watch List</h1>

<h3>Your lists</h3>
<% @lists.each do |list| %>
  <ul>
    <li><%= link_to list.name, list_path(list) %></li>
  </ul>
<% end %>
<br>
<%= link_to "+ add list", new_list_path, class: "btn btn-outline-primary" %>
